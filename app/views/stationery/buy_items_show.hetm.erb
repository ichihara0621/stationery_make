<% if stationeries.buy_items.receive = true  && stationeries.buy_items.send_status = true %>
    | <%= link_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>